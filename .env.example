PORT=8080
NODE_ENV=development

PGHOST=localhost
PGPORT=5432
PGDATABASE=backendoddohackathon
PGUSER=postgres
PGPASSWORD=postgres
# only needed when connecting to a server that requires SSL (e.g. AWS RDS verify-full)
DB_SSL_CA_PATH=

REDIS_URL=redis://localhost:6379

JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=1d

CLIENT_URL=http://localhost:3000
